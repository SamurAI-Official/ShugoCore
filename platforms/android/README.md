## Phase 4 — Direct agent-layer verification on Android devices

**Goal:** confirm that the newly added subsystems (memory, timers, dialogue,
fallback, tool execution) are functional on device, not just in tests.